# Plan 12-02: Add send-message and teacher-help mutation hooks

**Phase:** 12 - Chat Query and Mutation Hooks
**Status:** Complete

## Goal

Create mutation hooks for creating conversations, sending messages, and requesting teacher help.

## Tasks

- Add `src/hooks/chat/useCreateConversationMutation.ts`.
- Add `src/hooks/chat/useSendMessageMutation.ts`.
- Add `src/hooks/chat/useTeacherHelpMutation.ts`.
- Seed the conversation list/detail cache after successful conversation creation.
- Invalidate active conversation and conversation list on successful send.
- Keep teacher-help mutation focused on API request state.

## Verification

- Mutation hooks compile.
- No hook depends on mock data.
