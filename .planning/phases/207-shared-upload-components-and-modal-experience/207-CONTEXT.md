# Phase 207 Context

Upload UI must feel like a learning aid, not a file manager. The shared component set needs to support mobile photo capture, desktop drag/drop, browse fallback, previews, status, errors, retry, and accessible modal/inline patterns without dominating core learning pages.
