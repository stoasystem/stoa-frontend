# Phase 119 Plan

## Steps

1. Add centralized brand contact facts for platform reuse.
2. Add `StoaLogo`, `AppFooter`, `FooterContactInfo`, and `FooterLegalLinks`.
3. Wire the marketing layout footer and navigation to the new contact route.
4. Add contact namespace translations for English, German, French, and Italian.
5. Add contact API client, mutation hook, form components, and contact page.
6. Add a local backend `POST /contact/requests` API contract.
7. Document footer, logo, and contact form adaptation.
8. Verify JSON, TypeScript build, backend syntax, and newweb read-only status.
