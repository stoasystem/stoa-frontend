# Phase 298 Context

v8 concluded that `/home-v2` is visually stronger than the current homepage but blocked for route replacement. v9 starts from `docs/home-v2/v8-go-no-go-register.md` and is allowed to modify code and docs, while preserving the current `/` route.

The phase locks the acceptance contract: v9 clears blockers for switch-over planning only. It does not launch, replace `/`, claim legal finality, or approve final public photography.
