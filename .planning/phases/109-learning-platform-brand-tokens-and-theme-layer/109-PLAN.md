# Phase 109 Plan

## Goal

Implement derived learning-platform brand tokens and theme refinements.

## Tasks

1. Generate and verify `109-UI-SPEC.md`.
2. Add `src/styles/brand-tokens.css`.
3. Add `src/styles/platform-theme.css`.
4. Import the new theme files after existing themes.
5. Document token decisions in `docs/design/learning-platform-token-adjustment.md`.
6. Build and lint after token integration.

## Verification

- UI-SPEC approved.
- Tokens derive from homepage signals without copied source CSS.
- `npm run build` and `npm run lint` pass.
