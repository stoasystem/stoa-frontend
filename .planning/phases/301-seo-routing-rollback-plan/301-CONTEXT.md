# Phase 301 Context

v8 found generic `STOA Frontend` metadata and no Home V2-specific switch-over plan. v9 needs basic metadata now and a documented future route-switch plan, but must not actually switch `/`.

Image readiness also needs a formal temporary approval record.
