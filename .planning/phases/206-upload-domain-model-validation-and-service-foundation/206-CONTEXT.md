# Phase 206 Context

v2.2 needs upload behavior to be reusable across Chat, Question Bank, and Practice. Existing Chat upload behavior provided a starting point, but validation, preview lifecycle, handoff metadata, and service boundaries needed to move into a shared `src/features/uploads/` module.
