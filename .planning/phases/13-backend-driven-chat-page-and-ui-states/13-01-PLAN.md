# Plan 13-01: Update chat component props for backend data and disabled states

**Phase:** 13 - Backend-Driven Chat Page and UI States
**Status:** Complete

## Goal

Make existing chat components compatible with backend conversation summaries and mutation pending states.

## Tasks

- Update `ConversationSidebar` to accept `ConversationSummary[]`.
- Update `ConversationListItem` to accept `ConversationSummary`.
- Update `ChatInput` to accept `disabled`.
- Update `TeacherEscalationCard` to accept request handler, pending state, and optional feedback.

## Verification

- Components compile with backend summary/detail types.
- Existing visual structure remains intact.
