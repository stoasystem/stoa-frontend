# Phase 94 Summary

Completed.

Implementation:
- Public home components now read from `home` locale files.
- Auth/register components now read from `auth`, `common`, and `errors`.
- Language switcher appears on auth pages through the shared marketing layout.
- Register payload includes current i18n language as `preferredLanguage`.
