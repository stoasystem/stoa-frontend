# Plan 04-02: UI Primitives

**Phase:** 4 - Styling and UI Foundation
**Status:** Complete

## Goal

Add the first shadcn-style local UI primitive set.

## Tasks

- [x] Add `button`, `card`, `input`, `textarea`, `label`, `separator`, `badge`, `avatar`, `tabs`, `dialog`, `sheet`, `dropdown-menu`, and `scroll-area`.
- [x] Add supporting dependencies such as Radix primitives, `class-variance-authority`, `clsx`, and `tailwind-merge`.
- [x] Add `src/lib/utils.ts`.

## Verification

- TypeScript build passed with the new components.
- ESLint passed with the new components.
