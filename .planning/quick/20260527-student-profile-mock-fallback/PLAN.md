# Quick Task: Student Profile Mock Fallback

## Goal

Fix `/profile` showing `Failed to load profile.` in local mock mode.

## Plan

1. Add deterministic mock student profile data.
2. Wrap student profile GET/PATCH calls with demo fallback.
3. Verify build and browser rendering of `/profile`.
