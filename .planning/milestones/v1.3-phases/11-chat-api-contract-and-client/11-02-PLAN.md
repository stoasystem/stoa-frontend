# Plan 11-02: Refactor chat service endpoint functions

**Phase:** 11 - Chat API Contract and Client
**Status:** Complete

## Goal

Replace the obsolete `/chat` placeholder service with typed functions for the backend conversation and teacher-help endpoints.

## Tasks

- Import `httpClient` from `@/services/api/httpClient`.
- Implement `getConversations`.
- Implement `getConversation`.
- Implement `createConversation`.
- Implement `sendConversationMessage`.
- Implement `requestTeacherHelp`.
- Return `response.data` from each function.

## Verification

- No chat service function calls model provider APIs.
- No presentational component imports backend endpoint functions directly.
