# Plan 11-01: Update chat API type contracts

**Phase:** 11 - Chat API Contract and Client
**Status:** Complete

## Goal

Update `src/types/chat.ts` so the frontend has the complete Phase 4 backend chat contract.

## Tasks

- Replace or extend existing chat types with the supplied Phase 4 contract.
- Add `ConversationSummary` and `ConversationListResponse`.
- Add send-message, create-conversation, and teacher-help request/response types.
- Preserve existing `ChatRole`, `ChatMessageStatus`, `ChatMessage`, and `Conversation` compatibility where possible.

## Verification

- TypeScript build can resolve all exported chat types.
- Existing mock data remains type-compatible or downstream compile errors are intentionally handled in later Phase 13 work.
