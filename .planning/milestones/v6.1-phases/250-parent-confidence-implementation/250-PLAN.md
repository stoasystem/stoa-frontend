# Phase 250 Plan: Parent Confidence Implementation

## Goal

Ship the redesigned Parent Confidence section inside `/home-v2`.

## Scope

- Refactor component structure.
- Replace `points` copy with a `note` object in four locales.
- Add scoped CSS for parent frame, wash, note, and note marker.
- Extend Home V2 E2E coverage.

## Acceptance

- `/home-v2` renders the redesigned section.
- `/` remains unchanged.
- Copy is short and parent-first across four locales.
