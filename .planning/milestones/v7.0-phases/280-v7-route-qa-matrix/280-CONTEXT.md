# Phase 280 Context: Route QA Matrix

The user requested full-site multilingual adaptation. This requires route-level QA rather than checking only JSON coverage. Different route tiers need different review depth to keep the work practical.
