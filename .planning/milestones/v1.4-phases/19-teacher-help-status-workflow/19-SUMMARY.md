# Phase 19 Summary

## Completed

- Added `src/services/teacherHelp/teacherHelpApi.ts`.
- Updated `useTeacherHelpMutation` to use the dedicated teacher-help service.
- Added `useTeacherHelpStatusQuery`.
- Added `TeacherHelpStatusCard` and wired it into `/chat`.

## Verification

- Covered by `npm run build` and `npm run lint`.
