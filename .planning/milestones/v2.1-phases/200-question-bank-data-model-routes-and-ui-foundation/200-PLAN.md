# Phase 200 Plan

1. Add `src/types/questionBank.ts` with subject/topic/set/question/session/result/mistake/filter/context contracts.
2. Add `src/data/mockQuestionBank.ts` with deterministic subjects, topics, sets, questions, feedback, result, mistakes, and search helpers.
3. Add `src/services/questionBank/*` and `src/hooks/questionBank/*`.
4. Add route helpers in `src/lib/questionBankRoutes.ts`.
5. Wire route metadata and app routes for core Question Bank paths.
