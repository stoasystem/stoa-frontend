# Phase 133: Harness Provider Foundation and Codex Adapter - Context

**Gathered:** 2026-05-26
**Status:** Ready for planning
**Mode:** Auto-generated by autonomous workflow

<domain>
## Phase Boundary

Establish the local demo provider abstraction, Codex CLI adapter, template fallback, router, logging boundary, and ignored provider logs.

</domain>

<decisions>
## Implementation Decisions

- Keep provider logic in `demo-harness/harness/providers`.
- Use local `codex exec` as a demo-only bridge.
- Use template fallback as mandatory reliability protection.
- Do not expose provider details to frontend responses.
- Log provider events locally without sensitive payloads.

</decisions>

<code_context>
## Existing Code Insights

- FastAPI local backend exists under `backend/app`.
- Frontend chat API already calls backend endpoints and does not need provider-specific changes for Phase 133.
- `codex exec --help` confirms non-interactive mode and output capture flags.

</code_context>

<specifics>
## Specific Ideas

- ProviderRequest and ProviderResponse should be dataclasses.
- Codex provider should pass prompts through stdin and capture the final message from a temporary file.
- Router should fallback on exceptions and timeouts.

</specifics>

<deferred>
## Deferred Ideas

- Production OpenAI Responses API adapter is documented for handoff, not implemented in Phase 133.

</deferred>

