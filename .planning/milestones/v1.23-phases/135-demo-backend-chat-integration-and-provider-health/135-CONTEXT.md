# Phase 135: Demo Backend Chat Integration and Provider Health - Context

**Gathered:** 2026-05-26
**Status:** Ready for planning
**Mode:** Auto-generated by autonomous workflow

<domain>
## Phase Boundary

Connect the harness into the existing FastAPI demo backend while preserving frontend Chat API contracts and existing streaming compatibility.

</domain>

<decisions>
## Implementation Decisions

- Keep `SendMessageResponse` unchanged.
- Add `GET /health/provider` for internal QA.
- Load local student profile from SQLite and pass only grade, subjects, subject, question, and recent text context to the harness.
- Replace direct-answer/internal fallback copy with guided Learning Assistant language.

</decisions>

<code_context>
## Existing Code Insights

- `backend/app/main.py` owns the local `POST /conversations/{conversation_id}/messages` endpoint.
- Frontend chat service already uses `/conversations/{conversationId}/messages`.
- Existing stream endpoint wraps the non-streaming send result, so it remains compatible if send result content changes.

</code_context>

<specifics>
## Specific Ideas

- Insert `demo-harness` into `sys.path` from the backend entry point.
- Use `LearningAssistantRequest` as the boundary between FastAPI and the harness.

</specifics>

<deferred>
## Deferred Ideas

- No frontend provider fields or debug endpoint are added.

</deferred>

