# Phase 208 Context

Learning Chat is the primary destination for uploaded learning material. The composer already supports message sending, streaming responses, retry, and teacher-help status, so upload integration must preserve those behaviors while using the new shared upload foundation.
