# Plan 22-01: SQLite-Backed Local Test Backend

## Scope

- Add `backend/app` FastAPI service files.
- Create SQLite schema for users, profiles, relationships, conversations, messages, files, teacher help, and history.
- Add seed script and seed accounts.
- Add auth, student, parent, tutor, and conversation endpoints.
- Enforce 401/403 and role filtering.

## Verification

- `python3 -m py_compile backend/app/*.py`
- `PYTHONPATH=. python3 -m app.seed`
- Inspect SQLite tables and seeded users.
