# Plan 27-01: Phase 6 Documentation and Verification

## Scope

- Update README with Phase 6 auth, roles, routes, endpoints, token storage, local SQLite backend, and seed accounts.
- Verify frontend build and lint.
- Verify local SQLite seed and schema.
- Smoke check Vite route serving.

## Verification

- `npm run build`
- `npm run lint`
- `python3 -m py_compile backend/app/*.py`
- `PYTHONPATH=. python3 -m app.seed`
- `curl -I http://127.0.0.1:5173/login`
